[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "portfolio-movie-rating-production-style"
version = "1.0.0"
description = "Portfolio-grade movie rating regression project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "joblib",
  "matplotlib",
  "numpy",
  "pandas",
  "scikit-learn",
  "seaborn",
]

[project.scripts]
train-movie = "main:cli"

[tool.setuptools]
py-modules = ["main"]